<h1><%= @cocktail.name %></h1>
<%= cl_image_tag @cocktail.photo, height: 300, width: 400, crop: :fill %>
<!--<%= image_tag @article.photo.url(:bright_face) %>-->

Make this cocktail:
<ul>
  <% @cocktail.doses.each do |dose| %>
    <li>
      <%= dose.description %>
      <%= link_to 'Edit', edit_cocktail_dose_path(@cocktail, dose.id), class: "green" %>
      <%= link_to 'Delete', dose_path(dose), method: :delete, data: { confirm: 'Are you sure?' }, class: "green" %>
    </li>
  <% end %>
</ul>

Add a new dose:
<%= link_to 'Add dose', new_cocktail_dose_path(@cocktail), class: "green" %>

<%= link_to 'Edit', edit_root_path(@cocktail), class: "green" %>

<%= link_to 'Delete cocktail', @cocktail, method: :delete, data: { confirm: 'Are you sure?' }, class: "green" %>

<%= link_to 'All cocktails', cocktails_path, class: "green" %>
